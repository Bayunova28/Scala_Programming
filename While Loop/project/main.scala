// while loop
var stockPrice: Int = 200000
while(stockPrice <= 400000) {
  println(stockPrice);
  stockPrice = stockPrice + 100000
}

var hotelRevenue: Int = 500000;
while(hotelRevenue >= 100000) {
  println(hotelRevenue);
  hotelRevenue = hotelRevenue + 200000
}
